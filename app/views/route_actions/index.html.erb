<ul class="nav nav-tabs">
  <li class="nav-item">
    <a class="nav-link" href="<%= permissions_path %>">Permissions</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href="<%= features_path %>">Features</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href="<%= routes_path %>">Routes</a>
  </li>
  <li class="nav-item">
    <a class="nav-link active" href="<%= route_actions_path %>">Route Actions</a>
  </li>
</ul>
<div class="row mt-3 mb-3">
  <div class="col-sm">
    <div class="btn-toolbar mb-2 mb-md-0 float-right">
      <div class="btn-group mr-2">
        <%= link_to new_route_action_path, class: 'btn btn-secondary ml-2' do %>
          <span class="fa fa-plus"></span>
          New
        <% end %>
      </div>
    </div>
  </div>
</div>

<table class="table table-bordered">
  <thead class="thead-light">
    <tr>
      <th style="width: 20%">Feature</th>
      <th style="width: 10%">Route</th>
      <th style="width: 20%">Action/Method</th>
      <th style="width: 20%">Created At</th>
      <th style="width: 20%">Updated At</th>
      <th style="width: 20%">Action</th>
    </tr>
  </thead>

  <tbody>
    <% @route_actions.each do |route_action| %>
      <tr>
      <td><%= route_action.route.feature.name unless route_action.route.feature.nil? %></td>
        <td><%= route_action.route.path %></td>
        <td><%= route_action.action %></td>
        <td><%= route_action.created_at.strftime("%B %d, %Y %H:%M") %></td>
        <td><%= route_action.updated_at.strftime("%B %d, %Y %H:%M") %></td>
        <td>
          <%= link_to edit_route_action_path(route_action), class: 'btn btn-light btn-sm' do %>
            <span class="fa fa-file"></span>
            Edit
          <% end %>
          <%= link_to route_action, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-light btn-sm' do %>
            <span class="fa fa-trash"></span>
            Delete
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
