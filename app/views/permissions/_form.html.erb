<%= form_with(model: permission, local: true) do |form| %>
  <% if permission.errors.any? %>
    
      <% permission.errors.full_messages.each do |message| %>
        <div id="error_explanation" class="alert alert-warning alert-dismissable fade show" role="alert">
          <%= message.html_safe %>
          <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
      <% end %>
    
  <% end %>

  <div class="form-group row">
    <label for="permission_name" class="col-sm-1 col-form-label">
      Name *
    </label>
    <div class="col-sm-3">
      <%= form.text_field :name, required: true, class: "form-control" %>
    </div>
    <div class="col-sm-3"></div>
  </div>
  <div class="form-group row">
    <label class="col-sm-1 col-form-label">Authorizable ID *</label>
    <div class="col-sm-3">
      <%= form.text_field :authorizable_id, class: "form-control" %>
    </div>
  </div>
  <div class="form-group row">
    <label class="col-sm-1 col-form-label">Authorizable Type *</label>
    <div class="col-sm-3">
      <%= form.text_field :authorizable_type, class: "form-control" %>
    </div>
  </div>
  <div class="form-group row">
    <label class="col-sm-1 col-form-label">Permissible ID *</label>
    <div class="col-sm-3">
      <%= form.text_field :permissible_id, class: "form-control" %>
    </div>
  </div>
  <div class="form-group row">
    <label class="col-sm-1 col-form-label">Permissible Type *</label>
    <div class="col-sm-3">
      <%= form.text_field :permissible_type, class: "form-control" %>
    </div>
  </div>
  <div class="form-group row">
    <div class="col-sm-2">
      <div class="actions">
        <%= form.submit "Save" %>
      </div>
    </div>
  </div>
<% end %>
